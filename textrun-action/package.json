{
  "name": "textrun-action",
  "version": "0.7.0",
  "license": "ISC",
  "type": "module",
  "exports": "./dist/index.js",
  "files": [
    "dist/**/*.js"
  ],
  "scripts": {
    "build": "tsc -p tsconfig-build.json",
    "doc": "text-runner --format=dot",
    "fix": "biome check --write --unsafe . && dprint fmt && sort-package-json --quiet",
    "lint": "dprint check && sort-package-json --check --quiet && biome check --error-on-warnings && depcheck --config=../.depcheckrc",
    "reset": "rm -rf dist && yarn run build"
  },
  "dependencies": {
    "text-runner-engine": "7.5.0"
  },
  "devDependencies": {
    "text-runner": "7.5.0"
  }
}
