{
  "name": "custom-action-typescript",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "doc": "text-runner --format=progress",
    "fix": "prettier --write . && sort-package-json --quiet",
    "lint": "prettier -l . && sort-package-json --check"
  },
  "devDependencies": {
    "text-runner": "6.0.1",
    "tsx": "4.19.3"
  }
}
