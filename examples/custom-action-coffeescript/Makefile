build:

clean:

cuke:
cuke-lerna:

docs:
	echo custom-action-coffeescript documentation tests ...
	${CURDIR}/node_modules/.bin/text-run --format=progress

fix:

lint:

unit:

test: docs
test-lerna: test

.SILENT:
