{
  "name": "custom-action-esm",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "doc": "text-run --format=progress",
    "fix": "prettier --write .",
    "lint": "prettier -l ."
  },
  "devDependencies": {
    "text-runner": "6.0.0",
    "ts-node": "10.9.1"
  }
}
