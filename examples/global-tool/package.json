{
  "name": "global-tool-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "doc": "text-runner --format=dot",
    "fix": "biome check --write --unsafe && dprint fmt && sort-package-json --quiet",
    "lint": "biome check --error-on-warnings && dprint check && sort-package-json --check --quiet"
  },
  "dependencies": {
    "text-runner": "7.5.0",
    "textrun-shell": "0.7.0"
  },
  "devDependencies": {
    "tsx": "4.21.0"
  }
}
