// @flow

// returns a list of all available commands
module.exports = function (): string[] {
  return [
    'add',
    'debug',
    'dynamic',
    'help',
    'run',
    'setup',
    'static',
    'version'
  ]
}
