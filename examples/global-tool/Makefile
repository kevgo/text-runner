build:

clean:

cuke:
cuke-lerna:

doc:
	echo global-tool documentation tests ...
	${CURDIR}/node_modules/.bin/text-run --format=progress

fix:
	echo global-tool auto-fixes ...
	${CURDIR}/../../node_modules/.bin/prettier --write .

lint:
	echo global-tool linters ...
	${CURDIR}/../../node_modules/.bin/prettier -l .

unit:

test: doc
test-lerna: test

.SILENT:
