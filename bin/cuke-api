#!/usr/bin/env bash
set -e

bin/build
if [ "$#" != "1" ]; then
  NODE_ENV=test EXOSERVICE_TEST_DEPTH=API cucumber-js --tags '(not @clionly) and (not @todo)' --format progress "$@"
else
  NODE_ENV=test EXOSERVICE_TEST_DEPTH=API cucumber-js --tags '(not @clionly) and (not @todo)' "$@"
fi
