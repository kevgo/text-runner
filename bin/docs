#!/usr/bin/env bash
set -e

bin/build
if [ "$#" == "0" ]; then
  bin/text-run --exclude documentation/examples
else
  DEBUG='*,-babel,-text-stream-accumulator,-text-stream-search' bin/text-run --format detailed "$@"
fi
