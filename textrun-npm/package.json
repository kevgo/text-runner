{
  "name": "textrun-npm",
  "version": "0.6.0",
  "license": "ISC",
  "type": "module",
  "exports": "./dist/index.js",
  "files": [
    "dist/**/*.js"
  ],
  "scripts": {
    "build": "tsc -p tsconfig-build.json",
    "cuke": "yarn run build && cucumber-js --format=progress",
    "doc": "yarn run build && text-runner --format=dot",
    "fix": "eslint --fix --ignore-pattern=dist/ . && dprint fmt && sort-package-json --quiet && ../tools/rta ghokin fmt replace features",
    "lint": "dprint check && sort-package-json --check --quiet && eslint --ignore-pattern=dist/ . && depcheck --config=../.depcheckrc",
    "reset": "rm -rf dist && yarn run build",
    "unit": "node --test --import tsx 'src/**/*.test.ts'"
  },
  "dependencies": {
    "text-runner-engine": "7.4.0"
  },
  "devDependencies": {
    "shared-cucumber-steps": "*",
    "text-runner": "7.4.0",
    "textrun-action": "0.6.0",
    "textrun-extension": "0.6.0",
    "textrun-workspace": "0.6.0",
    "tsx": "4.20.6"
  }
}
