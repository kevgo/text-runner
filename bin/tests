#!/usr/bin/env bash

# RUNS THE UNIT TESTS
#
# Usage: `bin/tests [test-file]

bin/build

# it also uses the mocha options in test/mocha.opts
if [ $# == 0 ]; then
  node_modules/.bin/mocha --reporter dot "src/**/*-test.js"
else
  node_modules/.bin/mocha "$@"
fi
