build:
	@echo custom-action-sync build ...

clean:
	@echo custom-action-sync cleanup ...

cuke:
	@echo custom-action-sync E2E tests ...

docs:
	@echo custom-action-sync documentation tests ...
	@${CURDIR}/node_modules/.bin/text-run

fix:
	@echo custom-action-sync auto-fixes ...

lint:
	@echo custom-action-sync linters ...

unit:
	@echo custom-action-sync linters ...

test:
	@echo custom-action-sync tests ...
