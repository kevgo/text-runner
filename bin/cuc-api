#!/usr/bin/env bash

if [ "$#" != "1" ]; then
  EXOSERVICE_TEST_DEPTH=API cucumber-js --tags ~@todo "$@"
else
  EXOSERVICE_TEST_DEPTH=API DEBUG='*,-express:*,-body-parser:*,-finalhandler,-text-stream-search,-rails-delegate' cucumber-js --tags ~@todo "$@"
fi
