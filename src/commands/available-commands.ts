// returns a list of all available commands
export default function(): string[] {
  return [
    'add',
    'debug',
    'dynamic',
    'help',
    'run',
    'setup',
    'static',
    'version'
  ]
}
