/**
 * returns a list of all available commands
 */
export function availableCommands(): string[] {
  return [
    'add',
    'debug',
    'dynamic',
    'help',
    'run',
    'setup',
    'static',
    'version'
  ]
}
