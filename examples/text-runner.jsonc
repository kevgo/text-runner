// This file exists so that Text-Runner when run from these examples finds it and stops searching in parent directories.
// If it wouldn't exist, Text-Runner would use the text-runner.jsonc file in the root directory,
// which contains things that don't make sense here.
