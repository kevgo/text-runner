{
  "name": "custom-action-esm",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "doc": "text-runner --format=dot",
    "fix": "dprint fmt && sort-package-json --quiet",
    "lint": "dprint check && sort-package-json --check --quiet"
  },
  "devDependencies": {
    "text-runner": "7.5.0",
    "tsx": "4.21.0"
  }
}
