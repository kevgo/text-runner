files: "{*.md,documentation/**/*.md}"
exclude: "documentation/examples"
format: progress
useSystemTempDirectory: false

actions:
  runConsoleCommand:
    globals:
      "text-run": "src/bin/text-run"
