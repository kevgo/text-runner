{
  "name": "textrun-javascript",
  "version": "0.2.1",
  "license": "ISC",
  "type": "module",
  "exports": "./dist/index.js",
  "files": [
    "dist/**/*.js"
  ],
  "scripts": {
    "build": "tsc -p tsconfig-build.json",
    "clean": "rm -rf dist",
    "cuke": "cucumber-js --format=progress",
    "doc": "text-runner static --format=summary && text-runner dynamic --format=progress",
    "fix": "eslint --fix --ignore-pattern=dist/ . && prettier --write . && sort-package-json --quiet",
    "lint": "prettier -l . && sort-package-json --check --quiet && eslint --ignore-pattern=dist/ . && depcheck",
    "unit": "node --test --import tsx 'src/**/*.test.ts'"
  },
  "dependencies": {
    "text-runner-core": "7.0.0"
  },
  "devDependencies": {
    "shared-cucumber-steps": "*",
    "text-runner": "7.0.0",
    "textrun-extension": "0.2.1",
    "textrun-npm": "0.2.1",
    "tsx": "4.19.3"
  }
}
