#!/usr/bin/env bash

if [[ -L $0 ]]; then
  cliPath=$( dirname "$(readlink -f $0)" )/../dist/cli.js
else
  cliPath="$( dirname "${BASH_SOURCE[0]}")"/../dist/cli.js
fi
NODE_OPTIONS=--enable-source-maps node $cliPath "$@"
